# In Vercel: Project Settings -> Environment Variables -> add this one
# (name exactly as below), pointing at your deployed API Gateway's public
# URL with "/api" on the end. Redeploy after adding/changing it - Vite
# bakes environment variables in at build time, not read at runtime.
VITE_API_BASE_URL=https://<your-gateway-public-url>/api
